{
  "name": "@sobol/daemon",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "daemon:build-dev": "tsc --watch --project tsconfig.build.json",
    "daemon:run": "nodemon --inspect ./dist/index.js --watch dist/*"
  },
  "devDependencies": {
    "nodemon": "^2.0.12",
    "typescript": "^4.4.3"
  }
}
