{
  "name": "@sobol/daemon",
  "version": "1.0.0",
  "main": "index.js",
  "type": "commonjs",
  "private": true,
  "scripts": {
    "daemon:build-dev": "tsc --watch --project tsconfig.build.json",
    "daemon:run": "yarn run nodemon --inspect ./dist/index.js --watch dist/*",
    "daemon:deploy-commands": "yarn node ./dist/deploy-commands.js"
  },
  "devDependencies": {
    "@types/node": "^16.9.6",
    "nodemon": "^2.0.12",
    "typescript": "^4.4.3"
  },
  "dependencies": {
    "discord.js": "^13.1.0",
    "dotenv": "^10.0.0"
  }
}
